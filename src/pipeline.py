from .models import AnalysisResult

def run_analysis(*args, **kwargs) -> AnalysisResult:
    raise NotImplementedError(
        "Pipeline assembly starts after the real-image DeepForest smoke test passes."
    )
